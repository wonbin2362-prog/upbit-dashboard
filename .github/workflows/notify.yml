name: Discord Signal Notify

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Korean font
        run: sudo apt-get update && sudo apt-get install -y fonts-nanum

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run notifier
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
          DISCORD_WEBHOOK_URL_STOCK: ${{ secrets.DISCORD_WEBHOOK_URL_STOCK }}
          DISCORD_WEBHOOK_URL_BTC: ${{ secrets.DISCORD_WEBHOOK_URL_BTC }}
          DISCORD_WEBHOOK_URL_ETH: ${{ secrets.DISCORD_WEBHOOK_URL_ETH }}
          DISCORD_WEBHOOK_URL_WLD: ${{ secrets.DISCORD_WEBHOOK_URL_WLD }}
          DISCORD_WEBHOOK_URL_HYNIX: ${{ secrets.DISCORD_WEBHOOK_URL_HYNIX }}
        run: python notify.py

      - name: Commit alert state
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add alert_state.json signal_log.csv
          git diff --staged --quiet || git commit -m "Update alert state"
          git push
